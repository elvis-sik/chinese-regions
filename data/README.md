# Data

This directory contains the human-reviewed source data for the deck.

Main files:

- `raw/china_regions_notes_seed.csv`: the six region notes that drive the deck build
- `raw/region_image_sources.csv`: the media manifest for the shared blank map and locator SVGs

The goal is to keep this layer compact, readable, and easy to correct by hand.
