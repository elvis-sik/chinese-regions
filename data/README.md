# Data

`data/raw/region_image_sources.csv` is the current source-of-truth manifest for the verified Wikimedia region maps.

For now it records:

- the Commons file page
- a stable `Special:FilePath/...` direct asset URL
- format, nominal size, and license
- notes about visual caveats worth reviewing before deck publication
