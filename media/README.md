# Media

Downloaded deck media lives here.

Notes:

- `regions/` holds the fetched SVG files used in the final deck package.
- These files are generated by `scripts/fetch_region_media.py`.
- The media cache is intentionally not committed; the source manifest in `data/raw/region_image_sources.csv` is the canonical reference.
